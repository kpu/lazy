path-constant TOP : . ;
include $(TOP)/sanity.jam ;
boost 104200 ;
external-lib z ;

project : requirements $(requirements) <threading>multi:<define>WITH_THREADS ;
project : default-build <threading>multi <warnings>on <variant>release ;

build-project search ;
build-project alone ;

install-bin-libs alone//decode alone//just_vocab lm//build_binary ;
